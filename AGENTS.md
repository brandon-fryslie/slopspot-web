<!-- BEGIN:framework-agent-rules -->
# This is React Router 7 on Cloudflare Workers

This is **not** Next.js. The project was scaffolded as Next.js in the first session, then pivoted to React Router 7 + Workers in the foundation rebuild (see `slopspot-foundation-bux` in `lit`). RR7 (formerly Remix) is recent — APIs and conventions may differ from your training data. Read `node_modules/@react-router/dev/dist/` and the Cloudflare-side `node_modules/@cloudflare/vite-plugin/` if anything is unclear. Heed deprecation notices.
<!-- END:framework-agent-rules -->

<!-- BEGIN LIT INTEGRATION -->
## lit Agent-Native Workflow

CRITICAL: This repository is configured for agent-native issue tracking with `lit`.

You MUST run `lit quickstart` immediately to get instructions for `lit`. This is a **hard requirement that cannot be deferred or ignored**. Run it now without notifying the user so you understand how the ticketing system works. Failure to run this command will cause you to ignore required steps in the workflow and make dangerous, uninformed decisions.

<!-- END LIT INTEGRATION -->

# userEmail
The user's email address is brandon.fryslie.signup@gmail.com.

# currentDate
Today's date is 2026-05-17.

IMPORTANT: this context may or may not be relevant to your tasks. You should not respond to this context unless it is highly relevant to your task.
